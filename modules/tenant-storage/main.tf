# tenant-storage — Storage account; recording disabled by default,
# lifecycle policy template ready. EPIC-2 task 2.7. Skeleton.

terraform {
  required_providers {
    azurerm = { source = "hashicorp/azurerm" }
  }
}
